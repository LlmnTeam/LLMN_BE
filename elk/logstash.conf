input {
  beats {
    port => 5044
  }
}

filter {
  # UTC 타임스탬프를 KST로 변환
  date {
    match => ["@timestamp", "ISO8601"]
    timezone => "Asia/Seoul"
    target => "@timestamp"
  }

  # 로그 레벨 추출 로직
  grok {
    match => { "message" => [
      "(?<log_level>TRACE|DEBUG|INFO|NOTICE|WARN|WARNING|ERROR|SEVERE|FATAL)[:|\s]",
      "\[(?<log_level>TRACE|DEBUG|INFO|NOTICE|WARN|WARNING|ERROR|SEVERE|FATAL)\]",
      "(level|loglevel|log_level)=(?<log_level>TRACE|DEBUG|INFO|NOTICE|WARN|WARNING|ERROR|SEVERE|FATAL)"
    ]}
    break_on_match => true
    tag_on_failure => ["_grokparsefailure_loglevel"]
  }

  # 로그 레벨이 추출되지 않은 경우 기본값 설정
  if ![log_level] {
    mutate {
      add_field => { "log_level" => "UNKNOWN" }
    }
  }
  
  # 서버 IP 추출 및 저장
  if [host][ip] {
    mutate {
      add_field => { "server_ip" => "%{[host][ip][0]}" }
    }
  } else if [server][ip] and [server][ip] != "" {
    mutate {
      add_field => { "server_ip" => "%{[server][ip]}" }
    }
  } else {
    mutate {
      add_field => { "server_ip" => "unknown" }
    }
  }

  # 필요 없는 필드 제거, 하지만 server_ip는 유지
  mutate {
    remove_field => ["input", "@version", "tags", "log", "[container][labels]", "[container][image]", "[container][id]", "ecs", "agent", "stream"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "docker-logs-%{+YYYY.MM.dd}"
  }
}